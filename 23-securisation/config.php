<?php

// on va des constantes parce que c'est beau

const EMAIL_MOI = "email@email.com";
const RESULTATS_PG = 5;

const NOM_DE_PAGES = ['home','fonctions','contact','chatte']; //on a mis les noms de pages dans une constante qui sera vérifier par in_array() dans l'index
